#Install Dependencies - pip install -r requirements.txt, pip install reportlab, pip install azure-ai-formrecognizer==3.3.0, pip install azure-core==1.30.1, pip install azure-ai-documentintelligence, pip install openai, pip install azure-ai-inference

#Run the application - python resume_extractor_ui.py
